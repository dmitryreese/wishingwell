import { type FC } from "react";

import { JsonArray } from "./components/JsonArray";

const App: FC = () => {
    return (
        <div>
            <JsonArray />
        </div>
    );
};

export default App;
